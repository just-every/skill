# Local-only bindings for `wrangler dev`
# Copy to workers/api/.dev.vars and fill in per environment.

# D1 database binding as defined in wrangler.toml
DB=app-db

# R2 bucket binding
ASSETS=local-assets

# Authentication configuration
# Set this to your login worker origin (e.g., https://login.justevery.com or http://localhost:3000)
LOGIN_ORIGIN=https://login.justevery.com

# Optional integrations
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# JSON array of Stripe product/price IDs (optional)
# Example: STRIPE_PRODUCTS='[{"productId":"prod_ABC123","priceId":"price_XYZ789"}]'
STRIPE_PRODUCTS=

# Uncomment to override Expo defaults while pairing with the worker locally
#EXPO_PUBLIC_WORKER_ORIGIN=http://127.0.0.1:8787
