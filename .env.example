#############################################
# Project Identity & URLs
#############################################
# Required. Short slug for this deployment (letters/numbers/-/_ only).
PROJECT_ID=starter
# Required. Human-friendly project name used in generated copy.
PROJECT_NAME="Starter"
# Required. Public origin used for links, redirects, and fallbacks.
PROJECT_DOMAIN=https://starter.justevery.com
# App URL (defaults to ${PROJECT_DOMAIN}/app if omitted).
APP_URL=https://starter.justevery.com/app
# APP_BASE_URL=/app
# WORKER_ORIGIN=https://starter.justevery.com

#############################################
# Cloudflare (required)
#############################################
# Wrangler credentials (see docs/SECRETS_CLOUDFLARE.md).
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token
# CLOUDFLARE_ZONE_ID=your-zone-id

#############################################
# Logto (required for auth)
#############################################
LOGTO_ENDPOINT=https://login.justevery.com
LOGTO_ISSUER=https://login.justevery.com/oidc
LOGTO_JWKS_URI=https://login.justevery.com/oidc/jwks
# Auto-derivation fallback: defaults to ${PROJECT_DOMAIN}/api when omitted.
LOGTO_API_RESOURCE=https://starter.justevery.com/api
# Optional management credentials (required for provisioning in apply/env flows).
# LOGTO_MANAGEMENT_ENDPOINT=https://login.justevery.com
# LOGTO_MANAGEMENT_AUTH_BASIC=base64(client_id:secret)

#############################################
# Stripe (optional but recommended)
#############################################
# NEVER commit real keys. Use sk_test_* values for development.
# STRIPE_SECRET_KEY=sk_test_your_secret_key_here
# STRIPE_TEST_SECRET_KEY=sk_test_your_secret_key_here

# Product catalog definition. JSON format is recommended and supports
# descriptions, multiple prices, and metadata.
STRIPE_PRODUCTS='[{"name":"Founders","description":"Founders tier","prices":[{"amount":2500,"currency":"usd","interval":"month"}]},{"name":"Scale","description":"Scale tier","prices":[{"amount":4900,"currency":"usd","interval":"month"}]}]'

# Legacy semicolon format remains supported for backward compatibility.
# STRIPE_PRODUCTS="Founders:2500,usd,month;Scale:4900,usd,month"

# Optional. Override webhook URL (default: ${PROJECT_DOMAIN}/api/webhooks/stripe)
# STRIPE_WEBHOOK_URL=https://your-domain.com/api/webhooks/stripe
