# @justevery/config

Minimal environment helpers shared across the monorepo. The current helpers are
dependency-free and can be consumed by Workers or React Native clients to
sanity-check critical configuration before bootstrapping services.
